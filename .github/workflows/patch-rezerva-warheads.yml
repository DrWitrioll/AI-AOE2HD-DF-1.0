name: Patch Rezerva NII warheads

on:
  push:
    branches:
      - rezerva-web-assets
    paths:
      - '.github/workflows/patch-rezerva-warheads.yml'

permissions:
  contents: write

jobs:
  patch-site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout bridge branch
        uses: actions/checkout@v4
        with:
          ref: rezerva-web-assets
          fetch-depth: 0

      - name: Patch confirmed NII warheads
        run: node scripts/patch-rezerva-warheads.mjs

      - name: Commit updated site
        run: |
          git config user.name 'rezerva-site-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add rezerva-site/index.html
          git commit -m 'Add confirmed NII warhead registry' || exit 0
          git push origin HEAD:rezerva-web-assets

# trigger 2026-09-12 warhead-registry
